export function toErrorMessage(error: unknown, fallback = "Ocurrio un error inesperado."): string {
  if (error instanceof Error && error.message.trim()) {
    return error.message;
  }

  if (typeof error === "string" && error.trim()) {
    return error;
  }

  return fallback;
}

